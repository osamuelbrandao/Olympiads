\NeedsTeXFormat{LaTeX2e}
% Enables modern LaTeX compatibility.

% Name, date and version of the package.
\ProvidesPackage{article}[2025/09/08 v0.1 Olympiad Mathematics macros & enrironments]

% Specify how big is going to be the paper margins.
\RequirePackage[a4paper, margin=2.0cm]{geometry}

% amsmath: Add useful commands like aligh and gather.
% amsfonts: Add useful fonts like \mathbb{R}.
% amssymb: Add useful symbles like \therefore (needs amsfonts to work).
% amsthm: Add useful environments like \begin{proof}
\RequirePackage{amsmath, amsfonts, amssymb, amsthm, mathtools}

\usepackage{fontspec}
\usepackage{emoji}
\setmainfont{Latin Modern Roman}

\RequirePackage{xcolor}

\definecolor{wine}{HTML}{5f0f40}
\definecolor{red}{HTML}{9a031e}
\definecolor{amber}{HTML}{fb8b24}
\definecolor{burnt}{HTML}{e36414}
\definecolor{teal}{HTML}{0f4c5c}
\definecolor{white}{HTML}{fff7e5}
\definecolor{white2}{HTML}{f3e7d3}
\definecolor{white3}{HTML}{e0d0bc}
\definecolor{black}{HTML}{1e1528}

% Add Latin Modern Fonts like Sans-serif and Roman.
\RequirePackage{lmodern}

% Makes header and footer configurable.
\RequirePackage{fancyhdr}

\pagestyle{fancy}
\fancyhf{}

% Makes the use of colored and configured tables possible
\RequirePackage[most]{tcolorbox}

% Enables enumeration of items.
\RequirePackage{enumitem}

% Enables adding images.
\RequirePackage{graphicx}

% Enables cool hyper references.
\RequirePackage[colorlinks=true, linkcolor=teal, urlcolor=teal, citecolor=teal]{hyperref}

% Enables referencing theorems after its the declaration, for example.
\RequirePackage[nameinlink,capitalise]{cleveref}

% Enables book covering.
\RequirePackage{pdfpages}

\usepackage[portuges]{babel}

\usepackage{caption}

\newcommand{\olymp}{}
\newcommand{\olympiad}[1]{\renewcommand{\olympiad}{#1}}

\newcommand{\cyc}{\sum_{\text{cyc}}}
\newcommand{\sym}{\sum_{\text{sym}}}

\newcommand{\prodcyc}{\prod_{\text{cyc}}}
\newcommand{\prodsym}{\prod_{\text{sym}}}

\newcommand{\preface}{
	Este documento contém soluções para os problemas da \olympiad, escritos por mim durante minha
	preparação para a Olimpíada Internacional de Matemática.

	O conteúdo reflete meu próprio entendimento e processo de resolução de problemas. Algumas soluções
	podem ter sido inspiradas pelo trabalho de outras pessoas ou requerido ajuda externa; nesses casos,
	a devida atribuição é dada (veja a \cref{sec:references}).

	Se você notar algum erro ou tiver sugestões de melhoria, eu ficaria muito grato em recebê-las em \
	\href{mailto:samuelbaraujo19@gmail.com}{samuelbaraujo19@gmail.com}.
}

% Creates a new command, e.g. \RR works the same as mathbb{R}, but is easier.
\newcommand{\CC}{\mathbb{C}}
\newcommand{\RR}{\mathbb{R}}
\newcommand{\ZZ}{\mathbb{Z}}
\newcommand{\QQ}{\mathbb{Q}}
\newcommand{\NN}{\mathbb{N}}

\pagestyle{fancy}
\fancyhf{}

\fancyhead[L]{\olympiad}
\fancyhead[R]{\textcolor{color1}{Samuel Brandão}, \today}
\fancyfoot[C]{\thepage}
\setlength{\headheight}{14.5pt}

\tcbset{
	statementbox/.style={
			enhanced,
			drop shadow=white3,
			breakable,
			colback=white,
			colframe=amber,
			coltitle=white,
			colbacktitle=amber,
			fonttitle=\bfseries\sffamily,
			before skip=10pt, after skip=10pt,
			left=8pt, right=8pt, top=8pt, bottom=8pt,
			width=\linewidth,
			boxrule=0.6pt
		}
}

\tcbset{
	claimbox/.style={
			enhanced,
			breakable,
			colback=white,
			colframe=amber,
			coltitle=burnt,
			fonttitle=\bfseries\sffamily,
			before skip=10pt, after skip=10pt,
			left=8pt, right=8pt, top=8pt, bottom=8pt,
			width=\linewidth,
			attach title to upper,
			boxrule=0.6pt,
			after title={ --- }
		}
}

\tcbset{
	observationbox/.style={
			enhanced,
			breakable,
			colback=white,
			colframe=white,
			coltitle=burnt,
			fonttitle=\bfseries\sffamily,
			before skip=10pt, after skip=10pt,
			left=8pt, right=8pt, top=8pt, bottom=8pt,
			width=\linewidth,
			attach title to upper,
			boxrule=0.6pt,
			after title={ --- }
		}
}

\newtcolorbox{problem}{statementbox, title=Enunciado do Problema}
\newtcolorbox{claim}{claimbox, title=Afirmação}
\newtcolorbox{observation}{observationbox, title=Observação}

\crefname{section}{seção}{seções}
\Crefname{section}{Seção}{Seções}
