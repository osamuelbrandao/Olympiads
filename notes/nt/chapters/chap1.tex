\section{\textsf{Divisibility}}
\subsection{\textsf{Problems}}
\subsubsection{1.12.3}
\begin{problem}
Let $x,y,a,b,c$ be integers.
\begin{enumerate}
	\item Prove that $2x+3y$ is divisible by $17$ if and only if $9x+5y$ is divisible by $17$.
	\item If $4a+5b-3c$ is divisible by $19$, prove that $6a-2b+5c$ is also divisible by $19$.
\end{enumerate}
\end{problem}
\begin{enumerate}
	\item Say
	      \[
		      17 \mid 2x+3y \iff 17 \mid k(2x+3y).
	      \]
	      However, $k(2x+3y)-9x-5y=x(2k-9)+y(3k-5)$. Notice that $k=13$ results in $17x+34y$, which is
	      a multiple of $17$. Thus, if $2x+3y$ is divisible by $17$, $9x+5y$ is either. An analogous
	      solution proves the if and only if.
	\item Analogous argument proves the statement.
\end{enumerate}

\clearpage

\subsubsection{1.12.4}
\begin{problem}
Define the $n$th Fermat number $F_n$ by $F_n=2^{2^n}+1$. Show that $F_m,F_n$ are coprime for any $m,n$
\end{problem}
\begin{claim}
	$(2^{2^{m}}+1) \equiv 2 \pmod{(2^{2^{n}}+1)} \quad \forall m>n$.
\end{claim}
\begin{proof}
	Let $a=2^{2^{n}}$. Hence, we must prove
	\[
		a+1 \mid 2^{2^{m}}-1.
	\]
	Notice $2^{2^{m}}=(2^{2^{n}})^{2^{m-n}}=a^{2^{m-n}}$. Since $m<n$, say $a^{2^{m-n}}=2t$. Therefore, it suffices to prove that
	\[
		a+1 \mid a^{2^{m-n}}-1 = a^{2t}-1=(a+1)(a^{2t-1}+a^{2t-2}-\dots+1) \qedhere
	\]
\end{proof}
Therefore, $F_m$ and $F_n$ are clearly coprime.

\clearpage

\subsubsection{1.12.6 03/04/26}
\begin{problem}
Let $a,b$ be positive integers such that there exists a prime $p$ with the property
\[
	\operatorname{lcm}(a,a+p)=\operatorname{lcm}(b,b+p).
\]
Prove that $a=b$.
\end{problem}
\[
	\operatorname{lcm}(a,a+p)=\frac{a(a+p)}{\operatorname{gcd}(a,a+p)}=\frac{a(a+p)}{\operatorname{gcd}(a,p)}.
\]
Analogous argument for $\frac{b(b+p)}{\operatorname{gcd}(b,p)}$.
\begin{claim}
	\[
		\frac{a(a+p)}{\operatorname{gcd}(a,p)} = \frac{b(b+p)}{\operatorname{gcd}(b,p)} \iff a(a+p)=b(b+p).
	\]
\end{claim}
\begin{proof}
	AFTSOC $p$ divides only one of $a,b$. WLOG say $a>b$. Hence, $p \nmid b,b+p \iff p \nmid b(b+p)$. Thus,
	$p^2 \mid a(a+p)$, but $p^2 \nmid bp(b+p)$. Contradiction! $a(a+p)\neq bp(a+p)$ since those are not divisible
	by the same number. \qedhere
\end{proof}

\clearpage

\subsubsection{1.12.7 03/05/26}
\begin{problem}
Find all positive integers $n$ such that
\[
	3^{n-1}+5^{n-1}\mid 3^n+5^n.
\]
\end{problem}
\[
	3^n+5^n=3(3^{n-1}+5^{n-1})+ 2\cdot 5^{n-1}.
\]
However, $\operatorname{gcd}(3^{n-1},5^{n-1})=1$ for every positive integer $n$. Hence, the only possible
answer is $n=1$.
